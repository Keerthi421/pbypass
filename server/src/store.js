import fs from 'fs';import path from 'path';import {fileURLToPath} from 'url';const dir=path.join(path.dirname(fileURLToPath(import.meta.url)),'..','data');const file=path.join(dir,'interviewforge.json');fs.mkdirSync(dir,{recursive:true});const blank={users:[],bookmarks:{},progress:{},workspaces:{},mockSessions:{},content:{}};export function load(){try{return {...blank,...JSON.parse(fs.readFileSync(file,'utf8'))}}catch{return structuredClone(blank)}}let db=load();export const get=()=>db;export function save(){fs.writeFileSync(file,JSON.stringify(db,null,2));}export function mutate(fn){const out=fn(db);save();return out}